/**
 * 把时间戳格式化
 * @param ts
 * @param format
 */
export declare function timeFormat(ts: number, format: string): string;

/**
 * 把时间转换为用户友好的显示方式
 * @param ts
 */
export declare function timeFriendly(ts: number): string;

/**
 * 获取当前时间戳
 */
export declare function currentTimestamp(): number;
