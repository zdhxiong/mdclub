name: order
in: query
description: |
  排序方式。在字段前加 `-` 表示倒序排列。

  可排序字段包括 `create_time`、`follower_count`。默认为 `-create_time`
schema:
  type: string
  enum:
    - create_time
    - follower_count
    - '-create_time'
    - '-follower_count'
  default: -create_time
example: -create_time
