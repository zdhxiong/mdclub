name: hash
in: query
description: 用“,”分隔的图片hash，最多可提供 40 个 hash（IE 的 query 参数最长为 2k，为了不超过这个数值，限制最多可以提交 40 个 hash）
schema:
  type: array
  items:
    type: string
  minItems: 1
  maxItems: 40
style: form
explode: false
