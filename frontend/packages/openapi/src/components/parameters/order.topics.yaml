name: order
in: query
description: |
  排序方式。在字段前加 `-` 表示倒序排列。

  可排序字段包括 `topic_id`、`follower_count`。默认为 `topic_id`
schema:
  type: string
  enum:
    - topic_id
    - follower_count
    - '-topic_id'
    - '-follower_count'
  default: topic_id
example: topic_id
