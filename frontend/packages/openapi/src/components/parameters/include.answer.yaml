name: include
in: query
description: 包含的关联数据，用“,”分隔。可以为 `user`, `question`, `voting`
schema:
  type: array
  items:
    type: string
    enum:
      - user
      - question
      - voting
style: form
explode: false
example:
  - user
  - question
