required: true
content:
  application/json:
    schema:
      type: object
      properties:
        content_markdown:
          type: string
          description: Markdown 格式的正文
        content_rendered:
          type: string
          description: HTML 格式的正文
