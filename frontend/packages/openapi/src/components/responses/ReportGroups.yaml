description: 举报列表
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: integer
        data:
          $ref: '#/components/schemas/ReportGroups'
        pagination:
          $ref: '#/components/schemas/Pagination'
