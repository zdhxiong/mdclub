description: 评论列表
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: integer
        data:
          $ref: '#/components/schemas/Comments'
        pagination:
          $ref: '#/components/schemas/Pagination'
