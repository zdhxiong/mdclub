description: 话题列表
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: integer
        data:
          $ref: '#/components/schemas/Topics'
        pagination:
          $ref: '#/components/schemas/Pagination'
