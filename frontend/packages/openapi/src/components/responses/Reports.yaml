description: 举报详情列表
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: integer
        data:
          $ref: '#/components/schemas/Reports'
        pagination:
          $ref: '#/components/schemas/Pagination'
