description: 图片列表
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: integer
        data:
          $ref: '#/components/schemas/Images'
        pagination:
          $ref: '#/components/schemas/Pagination'
