type: object
properties:
  original:
    type: string
    description: 原图地址
  release:
    type: string
    description: 宽度固定，高度自适应的图片地址
  thumb:
    type: string
    description: 固定宽高的缩略图地址
required:
  - original
  - release
  - thumb
