type: object
properties:
  o:
    type: string
    description: 原图地址
  r:
    type: string
    description: 宽度固定，高度自适应的图片地址
  t:
    type: string
    description: 固定宽高的缩略图地址
required:
  - o
  - r
  - t
