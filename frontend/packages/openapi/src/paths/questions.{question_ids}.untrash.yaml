post:
  operationId: untrashMultiple
  summary: 🔐批量把提问移出回收站
  description: 批量把提问移出回收站。
  parameters:
    - $ref: '#/components/parameters/question_ids.path'
    - $ref: '#/components/parameters/include.question'
  responses:
    '200':
      $ref: '#/components/responses/Questions'
    default:
      $ref: '#/components/responses/Error'
  tags:
    - Question
