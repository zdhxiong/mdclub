post:
  operationId: untrash
  summary: 🔐把评论移出回收站
  description: 把评论移出回收站。
  parameters:
    - $ref: '#/components/parameters/comment_id.path'
    - $ref: '#/components/parameters/include.comment'
  responses:
    '200':
      $ref: '#/components/responses/Comment'
    default:
      $ref: '#/components/responses/Error'
  tags:
    - Comment
