get:
  operationId: get
  summary: 获取站点全局设置参数
  description: 获取站点全局设置参数。
  responses:
    '200':
      $ref: '#/components/responses/Option'
    default:
      $ref: '#/components/responses/Error'
  tags:
    - Option

patch:
  operationId: update
  summary: 🔐更新站点全局设置
  description: 更新站点全局设置。
  requestBody:
    $ref: '#/components/requestBodies/Option'
  responses:
    '200':
      $ref: '#/components/responses/Option'
    default:
      $ref: '#/components/responses/Error'
  tags:
    - Option
