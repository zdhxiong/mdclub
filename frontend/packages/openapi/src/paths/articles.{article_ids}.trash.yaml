post:
  operationId: trashMultiple
  summary: 🔐批量把文章放入回收站
  description: 批量把文章放入回收站。
  parameters:
    - $ref: '#/components/parameters/article_ids.path'
    - $ref: '#/components/parameters/include.article'
  responses:
    '200':
      $ref: '#/components/responses/Articles'
    default:
      $ref: '#/components/responses/Error'
  tags:
    - Article
