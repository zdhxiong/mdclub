post:
  operationId: trashMultiple
  summary: 🔐批量把回答放入回收站
  description: 批量把回答放入回收站。
  parameters:
    - $ref: '#/components/parameters/answer_ids.path'
    - $ref: '#/components/parameters/include.answer'
  responses:
    '200':
      $ref: '#/components/responses/Answers'
    default:
      $ref: '#/components/responses/Error'
  tags:
    - Answer
