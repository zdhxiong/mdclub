post:
  operationId: trashMultiple
  summary: 🔐批量把话题放入回收站
  description: 批量把话题放入回收站。
  parameters:
    - $ref: '#/components/parameters/topic_ids.path'
    - $ref: '#/components/parameters/include.topic'
  responses:
    '200':
      $ref: '#/components/responses/Topics'
    default:
      $ref: '#/components/responses/Error'
  tags:
    - Topic
