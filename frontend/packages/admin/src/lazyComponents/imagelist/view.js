import { h } from 'hyperapp';
import { JQ as $ } from 'mdui';
import './index.less';

export default () => (global_state, global_actions) => {
  const state = global_state.lazyComponents.imageList;
  const actions = global_actions.lazyComponents.imageList;

  return () => (

  );
};
